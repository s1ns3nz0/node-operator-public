# Deprecated: interactive-hoodi-release.sh does not load an environment file.
# Run scripts/release/node-operator-install.sh with no flags.
# It derives non-secret AWS account/Region from AWS CLI, GitHub numeric identity
# from gh CLI and git origin, and generates a bounded deployment name.
# The only initial prompts are for the user-owned withdrawal address and its
# confirmation; later custody ceremonies retain their own hidden prompts.
# Never place credentials, tokens, private keys, passwords, or recovery shares
# in this repository or an environment file.
